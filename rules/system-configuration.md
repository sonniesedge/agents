# System Config

- Use sonniesedge/laptop for configuring the system, including ZSH and brew
- `brew` is used to install and manage packages.
- Python is the preferred language for greenfield projects.
- Ensure environment variables used by agents are are reflected in both ~/.zshrc and ~/.zshenv
