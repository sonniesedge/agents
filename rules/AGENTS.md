# Personal Rules

These are my own rules, applied across every session on this machine. They are
not project rules — anything specific to a repo belongs in that repo's own
`AGENTS.md`.

## Token Budget

Before starting any major task, run `occe --output json` to check the remaining
token budget. Only proceed if there are enough tokens to complete the task.
