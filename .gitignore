# Private/work-internal configuration
/config.local.yaml

# Tool settings: these name internal hosts, services, and providers.
# Tracked docs stay, everything else is local.
/settings/*/*
!/settings/*/README.md

# Cloned remote skill repos
/.vendor/

# Rendered, prefixed skills (regenerated by `./scripts/agents.py sync`)
/.build/

.DS_Store
__pycache__/
*.pyc
